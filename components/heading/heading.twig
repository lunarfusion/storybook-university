{% block title_prefix %}{% endblock %}
<{{ html_tag|default('h2') }}{{ attributes.addClass(extra_classes|default([])) }}>
  {{ content }}
</{{ html_tag|default('h2') }}>
{% block title_suffix %}{% endblock %}
